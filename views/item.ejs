<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= item.title %></title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>
    <%- include('partials/header') %>

    <!-- Header displaying the item's title, category, and publication date -->
    <header class="container text-center my-4">
        <h1 class="display-4 text-primary"><%= item.title %></h1>
        <p class="text-muted">
            Category: <span class="badge bg-secondary"><%= item.category %></span>
        </p>
        <p class="text-muted">Published on: <%= item.postDate %></p>
    </header>

    <main class="container">
        <!-- Display the feature image if it exists -->
        <% if (item.featureImage) { %>
            <div class="text-center my-4">
                <img src="<%= item.featureImage %>" alt="<%= item.title %>" class="img-fluid rounded shadow">
            </div>
        <% } %>
        <!-- Display the item's content -->
        <div class="content">
            <p class="lead"><%= item.content %></p>
        </div>
    </main>

    <%- include('partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
